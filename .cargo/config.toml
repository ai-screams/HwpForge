# Cargo build configuration

[alias]
# Convenient aliases
xtask = "run --package bindings-cli --"
cov = "llvm-cov nextest --workspace --all-features"
lint = "clippy --workspace --all-targets --all-features -- -D warnings"
