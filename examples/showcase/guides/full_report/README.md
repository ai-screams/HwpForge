# Full Report

This bundle contains the finished report-style HWPX example and the Rust source
snapshot that generated it.

Artifacts:

- `full_report.hwpx`
- generator snapshot: `full_report.rs`

What to inspect:

- a larger end-to-end document
- sections, layout, tables, images, and report-like narrative flow in one file

Notes:

- `full_report.rs` is a copied snapshot of the canonical generator under
  `crates/hwpforge-smithy-hwpx/examples/`.
- The snapshot may still reference its original output path because it is kept
  unchanged for browsing.
