name: hwpforge
description: AI-native Korean HWP/HWPX document generation, inspection, and editing tools
startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "@hwpforge/mcp"],
      env: {}
    })
  exampleConfig: {}
