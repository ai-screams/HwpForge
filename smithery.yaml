name: hwpforge
description: AI-native Korean HWP/HWPX document generation, inspection, and editing tools
startCommand:
  type: stdio
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "@hwpforge/mcp"],
      env: {}
    })
  configSchema:
    type: object
    properties: {}
  exampleConfig: {}
